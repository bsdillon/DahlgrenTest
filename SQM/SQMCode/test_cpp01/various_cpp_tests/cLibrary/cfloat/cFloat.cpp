#include <cfloat>

int main()
{
    double x;

    x = FLT_ROUNDS;
    x = FLT_EVAL_METHOD;
    x = FLT_RADIX;
    x = FLT_MANT_DIG;
    x = DBL_MANT_DIG;
    x = LDBL_MANT_DIG;
    x = DECIMAL_DIG;
    x = FLT_DIG;
    x = DBL_DIG;
    x = LDBL_DIG;
    x = FLT_MIN_EXP;
    x = DBL_MIN_EXP;
    x = LDBL_MIN_EXP;
    x = FLT_MIN_10_EXP;
    x = DBL_MIN_10_EXP;
    x = LDBL_MIN_10_EXP;
    x = FLT_MAX_EXP;
    x = DBL_MAX_EXP;
    x = LDBL_MAX_EXP;
    x = FLT_MAX_10_EXP;
    x = DBL_MAX_10_EXP;
    x = LDBL_MAX_10_EXP;
    x = FLT_MAX;
    x = DBL_MAX;
    x = LDBL_MAX;
    x = FLT_EPSILON;
    x = DBL_EPSILON;
    x = LDBL_EPSILON;
    x = FLT_MIN;
    x = DBL_MIN;
    x = LDBL_MIN;

    return 0;
}