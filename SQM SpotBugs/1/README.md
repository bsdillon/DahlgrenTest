# SpotBugs plugin
To build this project right click on the `pom.xml` file, go to `run as`, and click on `Maven Install`.
This will build the new plugin jar file needed to load into SpotBugs. Refer to the `SpotBugs` document on
the V52 share drive for more information on how to setup your enviornment.
