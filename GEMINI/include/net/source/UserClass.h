#ifndef USERCLASS_H
#define USERCLASS_H

#include "abstractdriver.h"
#include "AbstractDriverFactory.h"

#include <memory>
#include <string>
#include <vector>

class UserClass
{
    /*
    public:
        std::unique_ptr<AbstractDriverFactory> aFactory;
        //static AbstractDriver aDriver;
        static std::string GetTopicAt(int index) {return aFactory.TopicList.at(index);}
        static std::unique_ptr<AbstractDriver> GetFactoryAtIndex(int index) {return std::move(aFactory.Factories.at(index));}
        static int GetTopicCount() {return aFactory.GetTopicCount();}
    */


};

#endif